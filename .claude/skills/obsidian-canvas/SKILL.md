---
name: obsidian-canvas
description: Create, edit, reorganize, validate, and maintain Obsidian .canvas files. Use when you need to turn notes, outlines, tables, protocols, architectures, or process descriptions into Obsidian Canvas nodes and edges; adjust Canvas layouts such as top-down trees or horizontal main splits with deeper detail levels; repair Canvas JSON; or inspect/validate .canvas structure.
---

# Obsidian Canvas (Claude Code pointer)

Canonical source of truth (Codex skill): `../../../obsidian-canvas/SKILL.md` (relative to this repo root).

When this skill triggers, **read that file and follow it fully**, together with any `references/`, `scripts/`, and `assets/` next to it. Don't restate its rules here — always defer to the canonical Codex `SKILL.md` so Codex and Claude Code stay in sync.

Notes:
- Ignore `agents/openai.yaml` (Codex-only metadata).
- This `.claude/skills/` tree mirrors the Codex skills of this repo as Claude Code pointers; the Codex `SKILL.md` files remain the single source of truth.
