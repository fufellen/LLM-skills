---
name: phd-lerer-repo
description: Maintain the user's `fufellen/phd_lerer` GitHub repository for PhD/Lerer calculation programs. Use when you need to move, add, validate, document, commit, or push C, Python, COMSOL Java, EDP/EIM, plasmonic-waveguide, or other reproducible scientific calculation code into `phd_lerer`, keep README files current, or decide which generated artifacts belong in Git.
---

# PhD Lerer Repo (Claude Code pointer)

Canonical source of truth (Codex skill): `../../../phd-lerer-repo/SKILL.md` (relative to this repo root).

When this skill triggers, **read that file and follow it fully**, together with any `references/`, `scripts/`, and `assets/` next to it. Don't restate its rules here — always defer to the canonical Codex `SKILL.md` so Codex and Claude Code stay in sync.

Notes:
- Ignore `agents/openai.yaml` (Codex-only metadata).
- This `.claude/skills/` tree mirrors the Codex skills of this repo as Claude Code pointers; the Codex `SKILL.md` files remain the single source of truth.
